{
  "$schema": "../../challenges.schema.json",
  "name": "Addition",
  "description": "Create a function `add` that return the **sum** of the two numbers passed as parameters\n\nNext line !",
  "functionName": "add",
  "params": ["a", "b"],
  "tests": [
    {
      "inputs": ["2", "2"],
      "expected": "4"
    },
    {
      "inputs": ["2", "3"],
      "expected": "5"
    }
  ]
}