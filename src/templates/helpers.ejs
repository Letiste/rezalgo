const helpers = {
<% helpers.forEach(({language, languageMap}) => { %>
  <%- language %>: `<%- languageMap.defFunctionStart({name: functionName, params: functionParams, returnType: functionReturnType}) %>
  
<%- languageMap.defFunctionEnd %>`, 
<% }) %>}

exports.helpers = helpers
