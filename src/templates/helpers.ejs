export const helpers = {
<% helpers.forEach(({language, languageMap}) => { %>
  <%- language %>: `<%- languageMap.defFunctionStart({functionName, params}) %>

<%- languageMap.defFunctionEnd %>`, 
<% }) %>}
